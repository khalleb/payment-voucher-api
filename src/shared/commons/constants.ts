
export enum Routes {
  USER = 'user',
}

export enum RoutesNameType {
  STORE = 'store',
  UPDATE = 'update',
  SHOW = 'show',
  DELETE = 'delete',
  INACTIVATE_ACTIVATE = 'inactivate-activate',
  INDEX = 'index',
}